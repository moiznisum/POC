{
    "name": "backend",
    "root": "apps/backend",
    "sourceRoot": "apps/backend",
    "projectType": "application",
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "cd apps/backend && go build -o instance -v"
        }
      },
      "serve": {
        "executor": "tools/executors/go:go",
        "options": {}
      }
    }
  }
  